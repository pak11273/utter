import {createReducer} from "../../utils/redux-utils.js"

const initialState = {
  name: "Robert E. Lee French Club",
  affiliation: "Robert E. Lee High School"
}
const hello = "hello"

export default createReducer(initialState, {})
