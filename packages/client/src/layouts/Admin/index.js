import {Grid, Header} from "semantic-ui-react"
import React from "react"

export default () => (
  <Grid>
    <Grid.Row centered style={{background: "black", padding: "100px"}}>
      <Header as="h3" color="yellow" textAlign="left">
        Will be resorting to react-admin soon!
      </Header>
    </Grid.Row>
  </Grid>
)
